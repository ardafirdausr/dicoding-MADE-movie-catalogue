<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ardafirdausr.movie_catalogue">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.ardafirdausr.movie_catalogue.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.ardafirdausr.movie_catalogue.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name=".provider.MovieProvider"
            android:authorities="com.ardafirdausr.movie_catalogue"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.ardafirdausr.movie_catalogue.READ_DATABASE"
            android:writePermission="com.ardafirdausr.movie_catalogue.WRITE_DATABASE" />

        <service
            android:name=".service.NotificationIntentService"
            android:exported="false" />

        <service
            android:name=".service.NotifyWidgetIntentService"
            android:exported="false" />

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <receiver android:name=".ui.widget.FavouriteMoviesWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/favourite_movies_widget_info" />
        </receiver>

        <activity
            android:name=".ui.activity.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.movieDetail.MovieDetailActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.MainActivity" />
        </activity>
        <activity
            android:name=".ui.activity.tvShowDetail.TvShowDetailActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.MainActivity" />
        </activity>

        <service
            android:name=".ui.widget.StackWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />
    </application>

</manifest>